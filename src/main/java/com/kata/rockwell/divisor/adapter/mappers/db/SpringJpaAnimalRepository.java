package com.kata.rockwell.divisor.adapter.mappers.db;

import org.springframework.data.jpa.repository.JpaRepository;

public interface SpringJpaAnimalRepository extends JpaRepository<Animal, Integer> {
}
